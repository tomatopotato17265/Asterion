{
  "name": "asterion",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that exchanges a Modrinth OAuth2 authorization code for an access token, keeping the client secret off the mobile client.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260905.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.129.0"
  }
}
